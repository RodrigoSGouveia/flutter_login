import 'package:flutter/material.dart';

class LoadingStatebody extends StatelessWidget {
  const LoadingStatebody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset("assets/indt.png"));
  }
}
